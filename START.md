# Starting Reef Bloom

Open a terminal in this folder and run:

```bash
npm install
npm run dev
```

Then open http://localhost:5173 in your browser.

For iPad testing on the same network:
```bash
npm run dev -- --host
```
Then open the Local IP shown on your iPad's Safari.
