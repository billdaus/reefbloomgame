{
  "name": "reef-bloom",
  "version": "0.0.1",
  "description": "Reef Bloom — ecosystem stewardship prototype",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "ios:sync": "vite build && cap sync ios",
    "ios:open": "vite build && cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0",
    "firebase": "^12.14.0",
    "pixi.js": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0",
    "sharp": "^0.35.1",
    "vite": "^5.0.0"
  }
}
